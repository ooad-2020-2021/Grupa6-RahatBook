@model IEnumerable<RahatBookAplikacija.Models.Rezervacija>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.idKorisnika)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idSJ)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dolazak)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.odlazak)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ekskurzija)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idKorisnika)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idSJ)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dolazak)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.odlazak)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ekskurzija)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
